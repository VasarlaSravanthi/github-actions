name: hello-world
# triggers
on: 
 push :
 workflow_dispatch:
# stages
jobs:
  my-job: #stage
    runs-on: ubuntu-latest # agent This is github hosted runner
    steps:
      - name: my-step-1 #step
        run: echo "Hello World!"

      - name: my-step -2 #step
        run: echo "Hello Future!"